{% set name = "spox" %}
{% set version = "0.10.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/spox-{{ version }}.tar.gz
  sha256: 893d6111dbbb4ba62760a5901fbb2eb1772121c2c236f9e40f4792e24aa8f092

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8.0
    - setuptools
    - setuptools-scm
    - wheel
    - pip
  run:
    - python >=3.8.0
    - numpy
    - onnx >=1.13

test:
  imports:
    - spox
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/quantco/spox
  summary: A framework for constructing ONNX computational graphs.
  dev_url: https://github.com/quantco/spox
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - cbourjau
    - janjagusch
